-- Auto-created by ranker.py / db.py on first connection.
-- This file ensures the database exists before the app starts.

-- The resume_ranking database is created by docker-compose via POSTGRES_DB.
-- Schema and tables are auto-migrated by the Python application.

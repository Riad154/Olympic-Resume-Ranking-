# PostgreSQL (Neon) — required for all deployments
PG_HOST = "your-neon-host.aws.neon.tech"
PG_PORT = "5432"
PG_DBNAME = "resume_ranking"
PG_USER = "your-neon-user"
PG_PASSWORD = "your-neon-password"

# Ollama (local only — not used on Streamlit Cloud)
OLLAMA_HOST = "http://localhost:11434"
OLLAMA_MODEL = "mistral"

# GitHub Actions trigger (required for BDJobs sync on Streamlit Cloud)
GH_TOKEN = "ghp_xxxxxxxxxxxxxxxxxxxx"      # GitHub Personal Access Token with repo + workflow scopes
GH_REPO = "Riad154/Olympic-Resume-Ranking-" # Your repo slug

# BDJobs credentials (used by GitHub Actions runner for headless login)
BDJOBS_USER = "your-bdjobs-username"
BDJOBS_PASS = "your-bdjobs-password"
